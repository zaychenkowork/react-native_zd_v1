// Shared enums used across the app
// (query keys are NOT enums — see src/docs/hooks-query.md for key factories)
// Example:
//
// export enum TransactionStatus {
//   Pending = 'pending',
//   Completed = 'completed',
// }
